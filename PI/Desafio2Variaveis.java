package JapaPI;

import java.util.Scanner;
/*
 */

public class Desafio2Variaveis {

    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);

        int maior = Integer.MIN_VALUE;
        int numero;

        //primeiro numero
        numero = sc.nextInt();
        if (numero > maior) {
            maior = numero;
        }
        //segundo numero
        numero = sc.nextInt();
        if (numero > maior) {
            maior = numero;
        }
        //terceiro numero
        numero = sc.nextInt();
        if (numero > maior) {
            maior = numero;
        }
        //quarto numero
        numero = sc.nextInt();
        if (numero > maior) {
            maior = numero;
        }
        //quinto numero
        numero = sc.nextInt();
        if (numero > maior) {
            maior = numero;
        }
        //sexto numero
        numero = sc.nextInt();
        if (numero > maior) {
            maior = numero;
        }
        //setimo numero
        numero = sc.nextInt();
        if (numero > maior) {
            maior = numero;
        }
        //oitavo numero
        numero = sc.nextInt();
        if (numero > maior) {
            maior = numero;
        }
        //nono numero
        numero = sc.nextInt();
        if (numero > maior) {
            maior = numero;
        }
        //decimo numero
        numero = sc.nextInt();
        if (numero > maior) {
            maior = numero;
        }
        //11
        numero = sc.nextInt();
        if (numero > maior) {
            maior = numero;
        }
        //12
        numero = sc.nextInt();
        if (numero > maior) {
            maior = numero;
        }
        //13
        numero = sc.nextInt();
        if (numero > maior) {
            maior = numero;
        }
        //14
        numero = sc.nextInt();
        if (numero > maior) {
            maior = numero;
        }
        //15
        numero = sc.nextInt();
        if (numero > maior) {
            maior = numero;
        //Fim    
        } else {
            System.out.println(maior);
        }
    }

}
